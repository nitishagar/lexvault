"""Detector — dictionary + regex span finder.

Phase 1: stub. The real detector (Aho-Corasick automaton built once at
construction, regex terms compiled once, ``find_matches``, longest-then-leftmost
overlap resolution, placeholder-namespace pre-exclusion) lands in Phase 2.
"""
