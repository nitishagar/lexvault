"""Engine — placeholder derivation + mask/unmask.

Phase 1: stub. The real engine (``derive_placeholder`` HMAC→base32, single-pass
left-to-right rebuild ``mask``, namespace-regex ``unmask``) lands in Phase 2.
"""
